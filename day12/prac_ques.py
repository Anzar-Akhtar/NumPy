import numpy as np

data = np.array([
    [20, 1, 25000],
    [25, 3, 40000],
    [30, 5, 60000],
    [35, 7, 80000]
])
adjustment = np.array([2, 1, 5000])

# print(data + adjustment)
# print(data - adjustment)

print(data.shape)
print(adjustment.shape)